package app.taskplanner.view;

import app.taskplanner.viewmodel.ViewHandler;

public class ViewFactory {
    ViewHandler viewHandler;
    public ViewFactory(ViewHandler viewHandler) {
       //nothing to do. Useless class
    }
}
