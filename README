W celu uruchomienia aplikacji należy sklonować repo, zbudować i następnie uruchomić plik StartApp (src/main/java/app/taskplanner/StartApp).
Aplikacja używa JDK 17.0.2, i wymaga JavaFX (u nas było zapewnione przez JDK i nie wymagało osobnej instalacji).
